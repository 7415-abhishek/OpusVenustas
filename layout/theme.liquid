<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ page_title }} – OpusVenustas</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="OpusVenustas – A curated fashion store where every piece feels like art. Wear your masterpiece.">
    {{ content_for_header }}

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <style>
      :root {
        --bg: #050607;
        --bg-soft: #0e1013;
        --accent: #e8c17b;
        --accent-soft: rgba(232, 193, 123, 0.12);
        --text: #f6f6f6;
        --muted: #a4a7af;
        --border-soft: #1e2127;
        --radius-lg: 18px;
        --radius-md: 12px;
        --shadow-soft: 0 18px 40px rgba(0, 0, 0, 0.35);
        --max-width: 1120px;
      }

      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: "Poppins", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
        background: radial-gradient(circle at top, #111421 0, #050608 45%, #020203 100%);
        color: var(--text);
        line-height: 1.6;
        -webkit-font-smoothing: antialiased;
      }

      img {
        max-width: 100%;
        display: block;
      }

      a {
        color: inherit;
        text-decoration: none;
      }

      button {
        font-family: inherit;
        border: none;
      }

      .page-wrapper {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      .container {
        width: 100%;
        max-width: var(--max-width);
        margin: 0 auto;
        padding: 0 1.25rem;
      }

      header {
        position: sticky;
        top: 0;
        z-index: 20;
        backdrop-filter: blur(18px);
        background: linear-gradient(to bottom, rgba(5, 6, 7, 0.92), rgba(5, 6, 7, 0.75), transparent);
        border-bottom: 1px solid rgba(255, 255, 255, 0.03);
      }

      .nav {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 1.1rem 0;
      }

      .nav-left {
        display: flex;
        align-items: center;
        gap: 0.75rem;
      }

      .logo-mark {
        width: 34px;
        height: 34px;
        border-radius: 999px;
        border: 1px solid var(--accent);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.85rem;
        font-weight: 600;
        letter-spacing: 0.08em;
      }

      .logo-text {
        display: flex;
        flex-direction: column;
        gap: 0.1rem;
      }

      .logo-text-title {
        font-size: 0.98rem;
        font-weight: 600;
        letter-spacing: 0.14em;
        text-transform: uppercase;
      }

      .logo-text-tagline {
        font-size: 0.7rem;
        text-transform: uppercase;
        letter-spacing: 0.19em;
        color: var(--muted);
        margin-bottom: 8px; /* extra space below tagline */
      }

      .nav-links {
        display: flex;
        align-items: center;
        gap: 1.75rem;
        font-size: 0.85rem;
        text-transform: uppercase;
        letter-spacing: 0.18em;
        margin-top: 10px; /* moves nav one step down */
      }

      .nav-links a {
        position: relative;
      }

      .nav-links a::after {
        content: "";
        position: absolute;
        left: 0;
        bottom: -0.4rem;
        width: 0;
        height: 1px;
        background: var(--accent);
        transition: width 0.25s ease;
      }

      .nav-links a:hover::after {
        width: 100%;
      }

      .nav-actions {
        display: flex;
        align-items: center;
        gap: 0.75rem;
      }

      .nav-icon-btn {
        min-width: 34px;
        height: 34px;
        border-radius: 999px;
        border: 1px solid var(--border-soft);
        background: rgba(10, 13, 18, 0.9);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.85rem;
        cursor: pointer;
        transition: border 0.2s ease, background 0.2s ease, transform 0.15s ease;
        padding: 0 0.75rem;
        gap: 0.35rem;
      }

      .nav-icon-btn:hover {
        border-color: var(--accent);
        background: rgba(26, 21, 11, 0.95);
        transform: translateY(-1px);
      }

      .nav-cta {
        padding: 0.45rem 1rem;
        border-radius: 999px;
        border: 1px solid var(--accent);
        background: radial-gradient(circle at top left, var(--accent) 0, #7b5c2c 32%, #161515 100%);
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 0.18em;
        cursor: pointer;
        transition: transform 0.16s ease, box-shadow 0.16s ease;
        box-shadow: 0 12px 30px rgba(0, 0, 0, 0.6);
      }

      .nav-cta:hover {
        transform: translateY(-1px);
        box-shadow: 0 18px 40px rgba(0, 0, 0, 0.7);
      }

      main {
        flex: 1;
      }

      .hero {
        padding: 4rem 0 3rem;
      }

      .hero-grid {
        display: grid;
        grid-template-columns: minmax(0, 1.15fr) minmax(0, 1fr);
        gap: 3rem;
        align-items: center;
      }

      .hero-kicker {
        display: inline-flex;
        align-items: center;
        gap: 0.55rem;
        padding: 0.18rem 0.6rem 0.18rem 0.18rem;
        border-radius: 999px;
        border: 1px solid rgba(255, 255, 255, 0.06);
        background: linear-gradient(
          90deg,
          rgba(232, 193, 123, 0.14),
          rgba(232, 193, 123, 0.04),
          transparent
        );
        font-size: 0.68rem;
        text-transform: uppercase;
        letter-spacing: 0.18em;
        color: var(--muted);
        margin-bottom: 1.4rem;
      }

      .hero-kicker-dot {
        width: 18px;
        height: 18px;
        border-radius: 999px;
        background: radial-gradient(circle at 30% 20%, var(--accent), transparent 70%);
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .hero-kicker-dot-inner {
        width: 7px;
        height: 7px;
        border-radius: 999px;
        background: #050505;
        border: 1px solid var(--accent);
      }

      .hero-title {
        font-size: clamp(1.9rem, 4vw, 2.4rem);
        line-height: 1.15;
        margin-bottom: 0.8rem;
        letter-spacing: 0.04em;
      }

      .hero-title span {
        background: linear-gradient(120deg, #ffffff, var(--accent));
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
      }

      .hero-subtitle {
        font-size: 0.94rem;
        color: var(--muted);
        max-width: 32rem;
        margin-bottom: 1.8rem;
      }

      .hero-subtitle b {
        color: #e9ebf3;
        font-weight: 500;
      }

      .hero-actions {
        display: flex;
        align-items: center;
        gap: 1.1rem;
        margin-bottom: 1.7rem;
        flex-wrap: wrap;
      }

      .btn-primary {
        padding: 0.75rem 1.75rem;
        border-radius: 999px;
        border: 1px solid var(--accent);
        background: radial-gradient(circle at top left, var(--accent) 0, #7b5c2c 40%, #18151a 100%);
        font-size: 0.85rem;
        text-transform: uppercase;
        letter-spacing: 0.18em;
        cursor: pointer;
        box-shadow: var(--shadow-soft);
        transition: transform 0.18s ease, box-shadow 0.18s ease;
      }

      .btn-primary:hover {
        transform: translateY(-1px);
        box-shadow: 0 22px 48px rgba(0, 0, 0, 0.8);
      }

      .btn-ghost {
        padding: 0.75rem 1.55rem;
        border-radius: 999px;
        border: 1px solid var(--border-soft);
        background: rgba(6, 7, 10, 0.9);
        font-size: 0.82rem;
        text-transform: uppercase;
        letter-spacing: 0.18em;
        cursor: pointer;
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        color: var(--muted);
        transition: background 0.18s ease, border-color 0.18s ease, color 0.18s ease, transform 0.18s ease;
      }

      .btn-ghost span.arrow {
        font-size: 1.05rem;
        transform: translateY(1px);
      }

      .btn-ghost:hover {
        border-color: var(--accent);
        background: rgba(28, 21, 10, 0.9);
        color: #f4f4f4;
        transform: translateY(-1px);
      }

      .hero-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 1.5rem;
        font-size: 0.72rem;
        text-transform: uppercase;
        letter-spacing: 0.22em;
        color: var(--muted);
      }

      .hero-meta span b {
        color: #f1f2f7;
        font-weight: 500;
      }

      .hero-visual-card {
        border-radius: 28px;
        border: 1px solid rgba(255, 255, 255, 0.05);
        background: radial-gradient(circle at top, #1e2130 0, #050507 50%, #020203 100%);
        box-shadow: 0 24px 60px rgba(0, 0, 0, 0.85);
        padding: 1.5rem 1.5rem 1.7rem;
        position: relative;
        overflow: hidden;
        isolation: isolate;
      }

      .hero-glow {
        position: absolute;
        inset: -40%;
        background:
          radial-gradient(circle at 0% 0%, rgba(232, 193, 123, 0.2), transparent 55%),
          radial-gradient(circle at 100% 30%, rgba(89, 179, 255, 0.2), transparent 60%);
        opacity: 0.65;
        pointer-events: none;
        z-index: -1;
      }

      .hero-pill {
        display: inline-flex;
        align-items: center;
        gap: 0.45rem;
        padding: 0.25rem 0.75rem;
        border-radius: 999px;
        border: 1px solid rgba(255, 255, 255, 0.08);
        background: rgba(5, 7, 12, 0.9);
        backdrop-filter: blur(12px);
        font-size: 0.72rem;
        color: var(--muted);
        margin-bottom: 0.9rem;
      }

      .hero-pill-dot {
        width: 8px;
        height: 8px;
        border-radius: 999px;
        background: #17e3a2;
        box-shadow: 0 0 14px rgba(23, 227, 162, 0.9);
      }

      .hero-look {
        border-radius: 24px;
        padding: 0.9rem;
        background: rgba(6, 8, 16, 0.88);
        border: 1px solid rgba(255, 255, 255, 0.08);
        display: grid;
        grid-template-columns: 0.85fr 1fr;
        gap: 0.85rem;
        margin-bottom: 1.1rem;
      }

      .look-photo {
        border-radius: 20px;
        background: radial-gradient(circle at 20% 0%, rgba(255, 255, 255, 0.2), transparent 55%),
          linear-gradient(160deg, #121217, #050506);
        aspect-ratio: 3 / 4;
        position: relative;
        overflow: hidden;
      }

      .look-photo::before {
        content: "OV";
        position: absolute;
        inset: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 3.4rem;
        font-weight: 600;
        letter-spacing: 0.18em;
        color: rgba(255, 255, 255, 0.06);
      }

      .look-photo-tag {
        position: absolute;
        bottom: 0.85rem;
        left: 0.85rem;
        padding: 0.2rem 0.7rem;
        border-radius: 999px;
        background: rgba(0, 0, 0, 0.78);
        border: 1px solid rgba(255, 255, 255, 0.18);
        font-size: 0.62rem;
        text-transform: uppercase;
        letter-spacing: 0.16em;
        color: var(--muted);
      }

      .look-meta-title {
        font-size: 0.85rem;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        margin-bottom: 0.15rem;
      }

      .look-meta-sub {
        font-size: 0.85rem;
        color: var(--muted);
        margin-bottom: 0.55rem;
      }

      .look-meta-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.45rem;
        margin-bottom: 0.7rem;
      }

      .tag-pill {
        font-size: 0.7rem;
        padding: 0.16rem 0.65rem;
        border-radius: 999px;
        border: 1px solid rgba(255, 255, 255, 0.08);
        background: rgba(5, 7, 12, 0.85);
        color: var(--muted);
      }

      .look-meta-footer {
        display: flex;
        align-items: center;
        justify-content: space-between;
        font-size: 0.72rem;
        color: var(--muted);
      }

      .look-meta-footer strong {
        font-size: 0.9rem;
        color: #f0f1f6;
      }

      .hero-mini-row {
        display: flex;
        align-items: stretch;
        gap: 0.85rem;
      }

      .hero-mini-card {
        flex: 1;
        border-radius: 18px;
        background: rgba(5, 7, 12, 0.92);
        border: 1px solid rgba(255, 255, 255, 0.08);
        padding: 0.75rem 0.9rem;
        font-size: 0.72rem;
        color: var(--muted);
      }

      .hero-mini-card strong {
        display: block;
        font-size: 0.82rem;
        color: #f4f4f4;
        margin-bottom: 0.2rem;
      }

      section {
        padding: 2.7rem 0 0;
      }

      .section-heading {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 1.5rem;
        margin-bottom: 1.7rem;
      }

      .section-title {
        font-size: 1.1rem;
        letter-spacing: 0.18em;
        text-transform: uppercase;
      }

      .section-subtitle {
        font-size: 0.82rem;
        color: var(--muted);
        max-width: 22rem;
      }

      .product-grid {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 1.5rem;
      }

      .product-card {
        border-radius: var(--radius-lg);
        background: var(--bg-soft);
        border: 1px solid var(--border-soft);
        padding: 0.85rem;
        display: flex;
        flex-direction: column;
        gap: 0.7rem;
        box-shadow: 0 10px 26px rgba(0, 0, 0, 0.55);
        transition: transform 0.18s ease, border-color 0.18s ease, box-shadow 0.18s ease;
        position: relative;
        overflow: hidden;
      }

      .product-card:hover {
        transform: translateY(-4px);
        border-color: rgba(232, 193, 123, 0.7);
        box-shadow: 0 18px 40px rgba(0, 0, 0, 0.9);
      }

      .product-label {
        position: absolute;
        top: 0.7rem;
        left: 0.7rem;
        font-size: 0.62rem;
        text-transform: uppercase;
        letter-spacing: 0.16em;
        padding: 0.16rem 0.6rem;
        border-radius: 999px;
        background: rgba(5, 7, 13, 0.9);
        border: 1px solid rgba(255, 255, 255, 0.2);
        color: var(--muted);
      }

      .product-image {
        border-radius: 16px;
        background: radial-gradient(circle at 20% 0%, rgba(255, 255, 255, 0.15), transparent 55%),
          linear-gradient(160deg, #151520, #050506);
        aspect-ratio: 3 / 4;
        margin-bottom: 0.55rem;
        position: relative;
        overflow: hidden;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .product-image::before {
        content: "Opus Edit";
        position: absolute;
        inset: 0.7rem;
        border-radius: 12px;
        border: 1px dashed rgba(255, 255, 255, 0.07);
        font-size: 0.7rem;
        text-transform: uppercase;
        letter-spacing: 0.3em;
        color: rgba(255, 255, 255, 0.06);
        display: flex;
        align-items: flex-end;
        justify-content: center;
        padding-bottom: 0.5rem;
      }

      .product-image img {
        position: relative;
        z-index: 1;
        width: 100%;
        height: 100%;
        object-fit: cover;
      }

      .product-info {
        display: flex;
        flex-direction: column;
        gap: 0.3rem;
      }

      .product-name {
        font-size: 0.9rem;
        font-weight: 500;
      }

      .product-meta {
        font-size: 0.74rem;
        color: var(--muted);
      }

      .product-bottom {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 0.6rem;
      }

      .product-price {
        display: flex;
        flex-direction: column;
        gap: 0.05rem;
      }

      .product-price-main {
        font-size: 0.98rem;
        font-weight: 500;
      }

      .product-price-note {
        font-size: 0.68rem;
        color: var(--muted);
      }

      .product-btn {
        padding: 0.55rem 0.9rem;
        border-radius: 999px;
        border: 1px solid var(--accent);
        background: linear-gradient(120deg, rgba(232, 193, 123, 0.12), rgba(232, 193, 123, 0.03));
        font-size: 0.72rem;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        cursor: pointer;
        white-space: nowrap;
        transition: background 0.18s ease, color 0.18s ease, transform 0.18s ease;
        color: var(--accent);
        text-align: center;
      }

      .product-btn:hover {
        background: radial-gradient(circle at top left, var(--accent) 0, #7b5c2c 45%, #18151a 100%);
        color: #050505;
        transform: translateY(-1px);
      }

      .story-grid {
        display: grid;
        grid-template-columns: minmax(0, 1.2fr) minmax(0, 1fr);
        gap: 2rem;
        align-items: start;
      }

      .story-text p {
        font-size: 0.9rem;
        color: var(--muted);
        margin-bottom: 0.9rem;
      }

      .story-text p b {
        color: #f0f2fa;
        font-weight: 500;
      }

      .story-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.45rem;
        margin-top: 0.4rem;
        margin-bottom: 1rem;
      }

      .story-tag {
        font-size: 0.7rem;
        padding: 0.16rem 0.6rem;
        border-radius: 999px;
        border: 1px solid rgba(255, 255, 255, 0.08);
        background: rgba(6, 7, 12, 0.9);
        color: var(--muted);
        text-transform: uppercase;
        letter-spacing: 0.18em;
      }

      .story-badges {
        display: flex;
        flex-wrap: wrap;
        gap: 0.8rem;
        margin-top: 0.6rem;
      }

      .story-badge {
        padding: 0.6rem 0.8rem;
        border-radius: 12px;
        border: 1px solid var(--border-soft);
        background: rgba(6, 8, 15, 0.95);
        font-size: 0.8rem;
        color: var(--muted);
        max-width: 11rem;
      }

      .story-badge strong {
        display: block;
        margin-bottom: 0.2rem;
        color: #f4f4f4;
      }

      .story-right-card {
        border-radius: var(--radius-lg);
        padding: 1.1rem 1.2rem;
        background: var(--accent-soft);
        border: 1px solid rgba(232, 193, 123, 0.4);
        color: #f8f2e7;
        box-shadow: var(--shadow-soft);
      }

      .story-right-card h3 {
        font-size: 0.98rem;
        text-transform: uppercase;
        letter-spacing: 0.16em;
        margin-bottom: 0.4rem;
      }

      .story-right-card p {
        font-size: 0.82rem;
        margin-bottom: 0.7rem;
      }

      .story-list {
        list-style: none;
        padding-left: 0;
        font-size: 0.8rem;
        display: grid;
        gap: 0.3rem;
      }

      .story-list li::before {
        content: "•";
        margin-right: 0.35rem;
      }

      .review-grid {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 1.3rem;
      }

      .review-card {
        border-radius: var(--radius-md);
        background: var(--bg-soft);
        border: 1px solid var(--border-soft);
        padding: 0.9rem;
        font-size: 0.78rem;
        color: var(--muted);
      }

      .review-stars {
        color: #ffd56b;
        font-size: 0.8rem;
        margin-bottom: 0.4rem;
      }

      .review-name {
        margin-top: 0.45rem;
        font-size: 0.8rem;
        font-weight: 500;
        color: #f2f2f2;
      }

      .review-note {
        font-size: 0.7rem;
        color: var(--muted);
      }

      .bottom-grid {
        display: grid;
        grid-template-columns: minmax(0, 1.2fr) minmax(0, 1fr);
        gap: 2rem;
        margin-top: 2.4rem;
        margin-bottom: 2.7rem;
      }

      .newsletter-card,
      .contact-card {
        border-radius: var(--radius-lg);
        background: var(--bg-soft);
        border: 1px solid var(--border-soft);
        padding: 1.2rem 1.25rem 1.45rem;
        box-shadow: var(--shadow-soft);
      }

      .newsletter-card h3,
      .contact-card h3 {
        font-size: 0.95rem;
        text-transform: uppercase;
        letter-spacing: 0.18em;
        margin-bottom: 0.4rem;
      }

      .newsletter-card p,
      .contact-card p {
        font-size: 0.8rem;
        color: var(--muted);
        margin-bottom: 0.95rem;
      }

      form {
        display: grid;
        gap: 0.55rem;
      }

      label {
        font-size: 0.75rem;
        color: var(--muted);
      }

      input,
      textarea {
        width: 100%;
        border-radius: 999px;
        border: 1px solid var(--border-soft);
        background: rgba(4, 6, 10, 0.96);
        padding: 0.65rem 0.8rem;
        color: var(--text);
        font-size: 0.8rem;
        outline: none;
        transition: border-color 0.16s ease, box-shadow 0.16s ease, background 0.16s ease;
      }

      textarea {
        border-radius: 12px;
        resize: vertical;
        min-height: 90px;
      }

      input::placeholder,
      textarea::placeholder {
        color: #656a77;
      }

      input:focus,
      textarea:focus {
        border-color: var(--accent);
        box-shadow: 0 0 0 1px rgba(232, 193, 123, 0.3);
        background: rgba(7, 9, 13, 1);
      }

      .form-row {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 0.6rem;
      }

      .btn-submit {
        margin-top: 0.4rem;
        padding: 0.7rem 1.1rem;
        border-radius: 999px;
        border: 1px solid var(--accent);
        background: radial-gradient(circle at top left, var(--accent) 0, #7b5c2c 40%, #18151a 100%);
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 0.18em;
        cursor: pointer;
        justify-self: flex-start;
        transition: transform 0.18s ease, box-shadow 0.18s ease;
        box-shadow: var(--shadow-soft);
      }

      .btn-submit:hover {
        transform: translateY(-1px);
        box-shadow: 0 18px 40px rgba(0, 0, 0, 0.8);
      }

      .contact-details {
        display: grid;
        gap: 0.4rem;
        font-size: 0.8rem;
        color: var(--muted);
      }

      .contact-details span {
        display: flex;
        align-items: center;
        gap: 0.4rem;
      }

      .contact-dot {
        width: 7px;
        height: 7px;
        border-radius: 999px;
        background: var(--accent);
      }

      footer {
        border-top: 1px solid var(--border-soft);
        padding: 1.3rem 0 1.6rem;
        font-size: 0.72rem;
        color: var(--muted);
      }

      .footer-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1.2rem;
        flex-wrap: wrap;
      }

      .footer-links {
        display: flex;
        flex-wrap: wrap;
        gap: 0.9rem;
      }

      .footer-links a {
        text-transform: uppercase;
        letter-spacing: 0.16em;
        font-size: 0.7rem;
      }

      .cart-badge {
        min-width: 19px;
        height: 19px;
        border-radius: 999px;
        background: var(--accent);
        color: #050505;
        font-size: 0.7rem;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        font-weight: 600;
      }

      @media (max-width: 900px) {
        .hero-grid,
        .story-grid,
        .bottom-grid {
          grid-template-columns: minmax(0, 1fr);
        }

        .hero {
          padding-top: 3rem;
        }

        .hero-visual-card {
          order: -1;
        }

        .section-heading {
          flex-direction: column;
          align-items: flex-start;
        }

        .product-grid,
        .review-grid {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }
      }

      @media (max-width: 720px) {
        .nav-links {
          display: none;
        }

        .hero {
          padding-top: 2.4rem;
        }

        .hero-grid {
          gap: 2.4rem;
        }

        .product-grid,
        .review-grid {
          grid-template-columns: minmax(0, 1fr);
        }

        .bottom-grid {
          margin-top: 2rem;
          margin-bottom: 2.3rem;
        }
      }

      @media (max-width: 480px) {
        .hero-title {
          font-size: 1.7rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="page-wrapper">
      <header>
        <div class="container nav">
          <div class="nav-left">
            <div class="logo-mark">OV</div>
            <div class="logo-text">
              <div class="logo-text-title">OpusVenustas</div>
              <div class="logo-text-tagline">Wear Your Masterpiece</div>
            </div>
          </div>

          <nav class="nav-links">
            <a href="#home">Home</a>
            <a href="#collection">Collection</a>
            <a href="#categories">Categories</a>
            <a href="#story">Story</a>
            <a href="#reviews">Reviews</a>
            <a href="#contact">Contact</a>
          </nav>

          <div class="nav-actions">
            <a class="nav-icon-btn" href="/search" aria-label="Search">
              🔍
              <span style="font-size: 0.75rem;">Search</span>
            </a>
            <a class="nav-icon-btn" href="/cart" aria-label="Cart">
              🛒
              {% if cart.item_count > 0 %}
                <span class="cart-badge">{{ cart.item_count }}</span>
              {% endif %}
            </a>
            <a class="nav-cta" href="#collection">
              Shop Now
            </a>
          </div>
        </div>
      </header>

      {{ content_for_layout }}

      <footer>
        <div class="container footer-row">
          <div>© {{ 'now' | date: '%Y' }} OpusVenustas. All rights reserved. All prices in INR.</div>
          <div class="footer-links">
            <a href="#home">Back to top</a>
            <a href="#collection">Shop</a>
            <a href="#story">Story</a>
            <a href="#contact">Contact</a>
          </div>
        </div>
      </footer>
    </div>
  </body>
</html>
